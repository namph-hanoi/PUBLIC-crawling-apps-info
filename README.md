Setup:
- Run a standalone chrome docker container following the description in https://hub.docker.com/r/selenium/standalone-chrome
- After setup the standalone chrome container above, Run 'python main.py' to manual test the script. Run 'pytest' for all the tests.
